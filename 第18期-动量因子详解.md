# 量化学习第18期：经典因子详解——动量因子

<callout type="primary">
本系列专注量化交易核心知识，从因子出发带你从零搭建量化体系。本期是经典因子详解的第一篇——动量因子。
</callout>

## 📌 一句话概括

**动量因子**（Momentum Factor）买入过去一段时间（通常是3-12个月）表现最好的股票、卖出表现最差的股票，利用**趋势持续性**获取超额收益——它是金融学界公认最稳健、也是争议最大的因子之一。

## 📖 概念详解

### 什么是动量效应

Jegadeesh 和 Titman（1993）在一篇里程碑式的论文中验证了一个简单却强大的现象：过去收益较高的股票（赢家）在未来一段时间内会继续跑赢过去收益较低的股票（输家）。这种"强者恒强、弱者恒弱"的效应被称为**动量效应**。

Carhart（1997）在此基础上，将动量因子正式纳入因子模型，在 Fama-French 三因子模型（市场、规模、价值）的基础上加入动量因子（MOM），提出 **Carhart 四因子模型**，显著提升了对股票截面收益的解释力。

### 动量因子的三种形态

学术界和业界发展出了三种主流的动量实现方式：

| 类型 | 原理 | 代表性论文 |
|------|------|-----------|
| **截面动量** | 在股票截面中排序，做多赢家做空输家 | Jegadeesh & Titman (1993) |
| **时序动量** | 比较单个资产自身的过去收益率，正则做多，负则做空 | Moskowitz et al. (2012) |
| **残差动量** | 先用因子模型剥离系统风险，对残差收益率排序 | Blitz et al. (2011) |

其中**截面动量**是最经典、最广泛使用的方式，也是本期重点。

### Carhart 四因子模型

Carhart（1997）动量因子（MOM）的构建方法：

1. 每个月末，计算每只股票过去 **t-12 到 t-2** 个月的累计收益率（跳过最近1个月，避免短期反转效应）
2. 按累计收益率将股票分成 **10 组（十分位数）**
3. **MOM = 赢家组合（前三组）等权平均收益 − 输家组合（后三组）等权平均收益**

<lark-table bordered>
  <thead>
    <tr>
      <th>变量</th>
      <th>说明</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>赢家组合</td>
      <td>过去收益率最高 30% 的股票</td>
    </tr>
    <tr>
      <td>输家组合</td>
      <td>过去收益率最低 30% 的股票</td>
    </tr>
    <tr>
      <td>回顾期</td>
      <td>过去 12 个月（跳过最近 1 个月）</td>
    </tr>
    <tr>
      <td>持有期</td>
      <td>通常 6-12 个月</td>
    </tr>
    <tr>
      <td>调仓频率</td>
      <td>月度调仓（重叠持有期）</td>
    </tr>
  </tbody>
</lark-table>

<callout type="warning">
⚠️ 动量因子最大的"陷阱"在于它的**崩溃风险**——在市场剧烈反转时（如2009年3月金融危机触底反弹），动量策略会遭受巨大回撤，因为跌幅最大的股票反弹最猛，而动量仍持有之前抗跌的"赢家"。Daniel & Moskowitz（2016）称之为"动量崩盘"。
</callout>

## 📐 数学公式

### 动量信号

$$
\text{Momentum}_{i,t} = \sum_{k=2}^{12} R_{i, t-k}
$$

即股票 i 在时间 t 的动量信号 = 过去第 2 个月到第 12 个月的累计收益率。**跳过 t-1 月**是为了规避 Jegadeesh（1990）发现的短期反转效应。

### Carhart 四因子模型

$$
R_{i,t} - R_{f,t} = \alpha_i + \beta_i (R_{m,t} - R_{f,t}) + s_i \text{SMB}_t + h_i \text{HML}_t + m_i \text{MOM}_t + \varepsilon_{i,t}
$$

其中 $\text{MOM}_t$ 是动量因子收益率（赢家减输家），$m_i$ 是股票 i 的动量因子暴露（动量 beta）。

### 动量策略收益分解

Ehsani & Linnainmaa（2022）证明，截面动量策略的期望收益可以分解为：

$$
E[\pi_t] = \text{cov}(\beta_{t-1}^{w-l}, \lambda_t) + \sum_{j=1}^{K} \text{cov}(\beta_{t-1}^{w-l,j}, \lambda_{j,t}) + \text{其他项}
$$

这意味着动量收益主要源于**因子收益的自相关性**——这和纯粹的行为金融学解释形成了有趣的互补。

## 🐍 Python 代码实战

下面用 Python 实现完整的动量因子构建与回测，使用 `yfinance` 获取真实数据：

```python
import numpy as np
import pandas as pd
import yfinance as yf
import matplotlib.pyplot as plt
from datetime import datetime, timedelta

# ============ 1. 获取数据 ============
def get_stock_data(tickers, start_date, end_date):
    """获取多只股票的历史价格"""
    data = yf.download(tickers, start=start_date, end=end_date, auto_adjust=True)
    if 'Close' in data.columns.names:
        close = data['Close']
    else:
        close = data
    return close

# 以道琼斯30成分股为例
tickers = [
    'AAPL', 'MSFT', 'JPM', 'JNJ', 'V', 'PG', 'UNH', 'HD', 'DIS', 'KO',
    'CSCO', 'BA', 'GS', 'IBM', 'MCD', 'CAT', 'AXP', 'NKE', 'WMT', 'TRV',
]

print("下载数据中...")
prices = get_stock_data(tickers, '2015-01-01', '2025-12-31')
returns = prices.pct_change()
print(f"数据范围: {prices.index[0].date()} ~ {prices.index[-1].date()}")
print(f"股票数量: {len(tickers)}")

# ============ 2. 构建动量因子 ============
def cross_sectional_momentum(prices, lookback=12, skip=1, holding=6):
    """
    截面动量因子构建

    参数:
        lookback: 回顾月数（默认12个月）
        skip: 跳过最近几个月（默认1个月，避免反转）
        holding: 持有月数（默认6个月）
    """
    # 计算月度收益率
    monthly_prices = prices.resample('ME').last()
    monthly_rets = monthly_prices.pct_change()

    # 构建动量信号：过去lookback个月的累计收益率（跳过skip个月）
    # 即 t-(lookback+skip) 到 t-skip 的累计收益率
    momentum_signal = (1 + monthly_rets).rolling(window=lookback).apply(
        lambda x: x.prod(), raw=True
    ).shift(skip + 1)  # 需要额外shift 1 避免未来信息

    # 实际上我们应该用：
    # 过去12个月的累计收益率，但跳过最近1个月
    # 即从 t-12 到 t-2 的累计
    momentum_signal = monthly_rets.rolling(window=lookback).apply(
        lambda x: np.prod(1 + x), raw=True
    )

    # 跳过skip个月（shift forward）
    momentum_signal = momentum_signal.shift(skip + 1)

    return momentum_signal, monthly_rets

# ============ 3. 形成投资组合 ============
def form_portfolios(momentum_signal, monthly_rets, num_groups=10):
    """
    按动量信号分组并计算组合收益

    参数:
        num_groups: 分组数（10 = 十分位数）
    """
    port_rets = pd.DataFrame(index=monthly_rets.index)

    for date in momentum_signal.index:
        if date not in monthly_rets.index:
            continue

        # 获取该月的动量信号
        signal = momentum_signal.loc[date].dropna()
        if len(signal) < num_groups:
            continue

        # 按动量排序并分组
        sorted_stocks = signal.sort_values()
        group_size = len(sorted_stocks) // num_groups

        for g in range(num_groups):
            start = g * group_size
            if g == num_groups - 1:
                stocks = sorted_stocks.index[start:]
            else:
                stocks = sorted_stocks.index[start:start + group_size]

            # 计算下一期的等权组合收益
            next_idx = monthly_rets.index.get_loc(date)
            if next_idx + 1 < len(monthly_rets):
                next_date = monthly_rets.index[next_idx + 1]
                port_rets.loc[next_date, f'G{g+1}'] = (
                    monthly_rets.loc[next_date, stocks].mean()
                )

    return port_rets

# 实际执行（简化版：用5分组代替10分组，因为只有20只股票）
momentum, rets = cross_sectional_momentum(prices)
portfolios = form_portfolios(momentum, rets, num_groups=5)

# ============ 4. 计算动量因子收益（赢家 - 输家） ============
portfolios = portfolios.dropna()
portfolios['MOM'] = portfolios['G5'] - portfolios['G1']  # 赢家 - 输家

# ============ 5. 绩效评估 ============
def performance_summary(returns, rf_annual=0.03):
    """打印绩效统计"""
    cum_ret = (1 + returns).cumprod()
    total_return = cum_ret.iloc[-1] - 1
    annual_return = (1 + total_return) ** (12 / len(returns)) - 1
    annual_vol = returns.std() * np.sqrt(12)
    sharpe = (annual_return - rf_annual) / annual_vol

    max_drawdown = (cum_ret / cum_ret.cummax() - 1).min()
    win_rate = (returns > 0).mean()

    print(f"{'='*50}")
    print(f"动量因子（MOM）绩效统计")
    print(f"{'='*50}")
    print(f"累计收益率:    {total_return:>8.2%}")
    print(f"年化收益率:    {annual_return:>8.2%}")
    print(f"年化波动率:    {annual_vol:>8.2%}")
    print(f"夏普比率:      {sharpe:>8.2f}")
    print(f"最大回撤:      {max_drawdown:>8.2%}")
    print(f"胜率(月):      {win_rate:>8.2%}")
    print(f"月均收益:      {returns.mean():>8.2%}")
    print(f"月收益标准差:  {returns.std():>8.2%}")

# ============ 6. 可视化 ============
mom_series = portfolios['MOM']
performance_summary(mom_series)

fig, axes = plt.subplots(2, 1, figsize=(14, 10))

# 累积收益曲线
cum_mom = (1 + mom_series).cumprod()
cum_spy = (1 + rets['SPY']).cumprod() if 'SPY' in rets.columns else None

axes[0].plot(cum_mom.index, cum_mom.values, label='Momentum Factor (Winners - Losers)',
             linewidth=2, color='#2196F3')
axes[0].axhline(y=1, color='gray', linestyle='--', alpha=0.5)
axes[0].set_title('Momentum Factor Cumulative Return', fontsize=14, fontweight='bold')
axes[0].set_ylabel('Cumulative Return')
axes[0].legend()
axes[0].grid(True, alpha=0.3)

# 月度收益率直方图
axes[1].hist(mom_series.dropna(), bins=30, color='#4CAF50', alpha=0.7, edgecolor='white')
axes[1].axvline(x=0, color='red', linestyle='-', linewidth=2, label='Zero Return')
axes[1].axvline(x=mom_series.mean(), color='blue', linestyle='--',
                linewidth=2, label=f'Mean: {mom_series.mean():.2%}')
axes[1].set_title('Momentum Factor Monthly Return Distribution', fontsize=14, fontweight='bold')
axes[1].set_xlabel('Monthly Return')
axes[1].set_ylabel('Frequency')
axes[1].legend()
axes[1].grid(True, alpha=0.3)

plt.tight_layout()
plt.savefig('momentum_factor_analysis.png', dpi=150, bbox_inches='tight')
plt.show()

print("\n✅ 动量因子分析完成！结果图已保存。")
```

<callout type="primary">
💡 上面的代码用到了 yfinance 获取真实市场数据。如果无法连接外网，也可以用 `pandas-datareader` 或本地的 CSV 替代。核心逻辑（动量计算、分组、做多赢家做空输家）是通用的。
</callout>

## 💡 实战应用

### 如何将动量因子用于实盘策略

<grid cols="2">
<div>
<b>📈 多因子组合</b>
<br/><br/>
动量因子通常与其他因子（价值、质量、低波）结合使用，形成互补策略：
<ul>
  <li>动量 + 质量：过滤出高动量但基本面健康的股票</li>
  <li>动量 + 低波：降低动量策略的高波动特性</li>
  <li>动量 + 价值：典型的"聪明贝塔"策略配置</li>
</ul>
</div>
<div>
<b>🛡️ 风险管理</b>
<br/><br/>
针对动量崩盘的常用方法：
<ul>
  <li>波动率缩放：按近期波动率调整头寸规模</li>
  <li>动态动量：用条件夏普比率动态调整</li>
  <li>趋势过滤：大盘指数处于下跌趋势时，暂停动量策略</li>
  <li>止损机制：对个股设置最大回撤止损</li>
</ul>
</div>
</grid>

### A股市场中的动量因子

研究表明，A股市场的动量效应与美股存在差异：

- **短期反转效应更强**：A股在1-3个月窗口期呈现显著的反转效应，而非动量
- **中期动量仍存在**：6-12个月窗口期也能观察到动量效应
- **行业动量明显**：A股行业层面的动量比个股层面更显著
- **需要做市值中性化**：A股动量因子与小市值因子相关性较高，需要做正交化处理

<callout type="success">
✅ 实务建议：在A股构建动量因子时，推荐使用 <b>t-12 至 t-3</b> 的累计收益率（跳过3个月），并在因子值上做市值和行业中性化处理，可以获得更纯净的动量暴露。
</callout>

## ⚠️ 常见陷阱

| 陷阱 | 说明 | 解决方案 |
|------|------|----------|
| **动量崩盘** | 市场剧烈反转时，动量策略单月可能亏损20%+ | 波动率缩放 + 趋势过滤 |
| **小市值偏差** | 动量组合容易集中在小市值股票 | 做市值中性化 |
| **幸存者偏差** | 回测时只用了当前幸存股票，忽略退市股票 | 使用无偏数据库（如CRSP） |
| **交易成本** | 动量策略换手率高，摩擦成本可能吞噬利润 | 降低调仓频率、使用延迟滑点模型 |
| **微观结构噪音** | 用日频数据计算月度动量，受买卖价差干扰 | 用周频或月频收益率，跳过最近期数据 |
| **过拟合** | 调整回顾期、持有期、分组数来拟合历史数据 | 在样本外数据验证，保持参数简单 |

<callout type="warning">
🎯 <b>核心教训：</b>动量的最大敌人不是市场，而是你自己——当动量策略连续3个月亏损时，你能否坚持不加"优化"？绝大多数动量策略的失败来自使用者过早放弃。
</callout>

## 🎯 今日小练习

为巩固今天学到的知识，请尝试以下练习：

1. **基础级**：使用 Python 计算任意 5 只 A 股过去 6 个月的动量，构建简单的动量排序策略
2. **进阶级**：在 A 股全市场截面计算动量因子（t-12 到 t-2），构建十分位数组合，计算 MOM 因子的年化收益和夏普比率
3. **挑战级**：实现一个"动量崩溃预警系统"——监测市场波动率（VIX）和动量策略的近期回撤，当两个指标同时超阈值时，降低动量暴露

<callout type="success">
💡 <b>小提示：</b>动量因子的研究已经超过30年，但它仍然是最活跃的量化研究领域之一。如果你想深入研究，推荐阅读 Jegadeesh & Titman (1993) 的原著和 Moskowitz et al. (2012) 关于时序动量的论文。读懂这两篇，你已经超过90%的动量交易者。
</callout>

## 🔗 参考来源

1. **Jegadeesh, N. & Titman, S. (1993)** — Returns to Buying Winners and Selling Losers: Implications for Stock Market Efficiency. *Journal of Finance*. [经典原著，发现动量效应的里程碑论文]
   - https://onlinelibrary.wiley.com/doi/abs/10.1111/j.1540-6261.1993.tb04702.x

2. **Carhart, M. M. (1997)** — On Persistence in Mutual Fund Performance. *Journal of Finance*. [提出 Carhart 四因子模型，将动量正式纳入因子体系]
   - https://onlinelibrary.wiley.com/doi/abs/10.1111/j.1540-6261.1997.tb03808.x

3. **BigQuant 动量因子30年综述** — 系统梳理了动量效应30年的学术研究和实践演进，中文精品
   - https://bigquant.com/wiki/doc/2TpWrLWwQZ

4. **Moskowitz, T. J., Ooi, Y. H., & Pedersen, L. H. (2012)** — Time series momentum. *Journal of Financial Economics*. [提出时序动量概念]
   - https://www.sciencedirect.com/science/article/pii/S0304405X12000228

5. **Daniel, K. & Moskowitz, T. J. (2016)** — Momentum crashes. *Journal of Financial Economics*. [解释动量崩盘的原因和应对策略]
   - https://www.sciencedirect.com/science/article/pii/S0304405X16300134

<callout type="primary">
---
<b>下期预告：</b>经典因子详解之二——价值因子（Value Factor）。我们将深入 PE、PB、PS 等估值指标的因子化，以及 Fama-French 模型中 HML 因子的构建方法。
</callout>
